<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppSchoolMaui.Pages.ProfilePage"
             Title="ProfilePage">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">
            <Grid HorizontalOptions="Center" WidthRequest="120" HeightRequest="120">
                <Image Aspect="AspectFill" Source="{Binding Photo}">
                    <Image.Clip>
                        <EllipseGeometry Center="60,60" RadiusX="60" RadiusY="60"/>
                    </Image.Clip>
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding PickPhotoCommand}"/>
                    </Image.GestureRecognizers>
                </Image>
                <Border BackgroundColor="#66000000" StrokeShape="RoundRectangle 16"
                HorizontalOptions="End" VerticalOptions="End" Padding="4" Margin="4">
                    <Label Text="📷" FontSize="12" TextColor="White"/>
                </Border>
            </Grid>

            <Entry Placeholder="Full name" Text="{Binding FullName}"/>
            <Entry Placeholder="Photo URL" Text="{Binding PhotoUrlRaw}" IsVisible="False"/>
            <Button Text="Save" Command="{Binding SaveCommand}"/>

            <Frame Padding="12" HasShadow="True">
                <VerticalStackLayout Spacing="4">
                    <Label Text="{Binding Email}"/>
                    <Label Text="{Binding Role}"/>
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
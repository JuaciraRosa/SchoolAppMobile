<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchoolAppMobile.Views.SubjectContentsPage"
             Title="Contents">
    <VerticalStackLayout Padding="16" Spacing="12">
        <Label x:Name="Header" FontSize="20" FontAttributes="Bold" />
        <ActivityIndicator x:Name="Loading" IsRunning="False" IsVisible="False" />
        <Label x:Name="EmptyLabel" Text="No content available." IsVisible="False" HorizontalOptions="Center" />
        <RefreshView x:Name="Refresh" IsRefreshing="False">
            <CollectionView x:Name="ContentsList">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="12" Margin="4" BorderColor="LightGray">
                            <VerticalStackLayout>
                                <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="18"/>
                                <Label Text="{Binding Body}" FontSize="13" LineBreakMode="WordWrap" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </VerticalStackLayout>
</ContentPage>
